#include <stdio.h>

int main() {
    int angka;
    printf("Masukkan angka: "); //fixed
    if (scanf("%d", &angka) != 1) {  //cek input 
        printf("Error: Not a number!\n");
        return 1;
    }
    printf(angka % 2 == 0 ? "EVEN\n" : "ODD\n"); //ubah code
    return 0;
}
